[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --workspace=server --include-workspace-root"]

[phases.build]
cmds = ["echo 'Skipping client build - backend only'"]

[start]
cmd = "npm start"
